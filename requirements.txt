# Core runtime dependencies
numpy
pandas>=0.21
scikit-learn>=0.21.2
scipy>=1.0
torch>=1.10.1
tensorboard>=2.7.0
tqdm
matplotlib>=3.0
h5py>=2.10.0
pysam
pyBigWig

# Optional dependencies (install as needed)
# explain mode:
# captum
# tangermeme
#
# notebook execution:
# nbformat
# nbclient
# ipykernel
#
# hyperparameter tuning:
# ray[tune]>=1.12.0
#
# llm / genome foundation-model modules:
# transformers>=4.0.0
# einops>=0.6.0
# evo2 and gpn-family packages are model-specific optional runtimes;
# install them from upstream instructions only when those adapters are used.
